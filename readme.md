// Thêm .env của backend
backend/.env
{
JWT_SECRET="samaelqt"
STRIPE_SECRET_KEY="thêm Secret key của STRIPE"
}

// tải các thư viện của admin, backend, frontennpm i

// lệnh chạy frontend
npm run dev

// lệnh chạy backend
npm run server

// lệnh chạy admin
npm run dev
